name: Release

on:
  push:
    branches: [master]
    paths:
      - "**/*.go"
      - "go.mod"
      - "go.sum"
      - "migrations/**"
  workflow_dispatch:

jobs:
  ci:
    name: CI Gate
    uses: ./.github/workflows/ci.yml
    permissions:
      contents: read
      security-events: write
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  release:
    name: Semantic Release
    needs: ci
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"

      - name: Build binary
        run: |
          VERSION=$(git describe --tags --always)
          go build -ldflags "-X main.version=${VERSION}" -o bin/server ./cmd/server/...

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: server-binary
          path: bin/server
          retention-days: 5

      - uses: actions/setup-node@v6
        with:
          node-version: lts/*

      - uses: go-semantic-release/action@2e9dc4247a6004f8377781bef4cb9dad273a741f
        id: semrel
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Update sonar project version
        if: steps.semrel.outputs.version != ''
        run: |
          sed -i "s/^sonar.projectVersion=.*/sonar.projectVersion=${{ steps.semrel.outputs.version }}/" sonar-project.properties
